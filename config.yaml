# X Virality Optimization Engine Configuration
# Set secrets via environment variables:
#   X_BEARER_TOKEN, ANTHROPIC_API_KEY, FLASK_SECRET_KEY

x_api:
  # Set via X_BEARER_TOKEN environment variable
  bearer_token: ""

auth:
  allowed_handles:
    - "lovejesusyt"
  secret_key: ""

allowed_origins: []

storage:
  db_path: "data/virality.db"

nlp:
  embedding_dim: 384
  try_real_embeddings: true

model_path: null
